# Supabase Migrations

This folder contains the database migrations for Supabase.

You can use the Supabase CLI to generate new migrations when you make changes to your local database or remote database.

For example, to pull the remote schema changes into a new migration file:
```bash
npx supabase db pull
```
